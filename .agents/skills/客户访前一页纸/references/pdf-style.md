<!-- resource-id: cisp://skill/client-pre-visit-one-pager/pdf-style -->
<!-- resource-version: 0-dev -->
<!-- source-skill-version: v5.8-embedded-pdf-style -->

# 客户访前一页纸 PDF 样式与交付契约

本文件是客户访前报告 PDF 排版与验收的完整事实源。页面、字体、字号、颜色、表格、分页、页眉页脚、结构检查、逐页渲染、失败清理，以及客户访前专属标题、段落、列宽和语义验收均在本文件定义。只运行本 Skill 的 `../scripts/report_pdf_toolkit.py`，不得读取或导入其他 Skill 的 PDF 资源。

## 内容索引

- [页面与基础 token]
- [中文字体契约]
- [表格与紧凑字段]
- [自然分页]
- [验收与交付]
- [本地工具]
- [内容与层级]
- [客户访前专属段落]
- [专属粗体白名单]
- [表格列定义]
- [专属语义验收]
- [文件与回退]

## 页面与基础 token

- 页面固定为 Letter，215.9 × 279.4 mm；上、下边距 20 mm，左、右边距 25 mm；可用正文宽度 165.9 mm。
- 正文使用宋体或等价中文宋体 Regular，10.5 pt、固定行高 15 pt、段后 4 pt。
- 表格正文使用 9 pt、固定行高 12 pt；元信息使用 9 pt、固定行高 12 pt。
- 主标题使用真实中文标题字形，18 pt、黑色、粗体视觉、居中。
- 一级标题 14 pt，二级标题 12 pt；均使用真实中文标题字形与 `#4D6EEB`。一级标题段前 12 pt、段后 6 pt；位于页首时不额外制造空白。引用块竖线等非文字章节装饰同样使用 `#4D6EEB`。
- 表头使用主色的低饱和浅色 `#CED4EE` 实色底纹，文字黑色、真实中文 Bold、左对齐；普通数据行不得使用该底色。
- 表格使用黑色 0.5 pt 全网格；正文 Regular、左对齐、垂直居中。
- 不添加通用封面。每页统一绘制报告家族页眉页脚，且只能占用现有页边距，不得侵入或挤压正文区：页眉左侧为蓝色真实 Bold“水滴征信 MCP”，右侧为深灰色“审计留档”，下方绘制 0.5 pt 蓝色分隔线；页脚左侧为深灰色“cisp.zenitera.com · 水滴征信 MCP”，右侧为“第 N 页”，上方绘制 0.5 pt 浅灰分隔线。
- 页眉页脚左右边界与正文区一致，均为距页面左右侧 25 mm；页眉文字基线距页顶 9 mm、分隔线距页顶 12 mm，页脚分隔线距页底 12 mm、文字基线距页底 7.5 mm。页眉左侧品牌文字和页眉分隔线固定为 `#4D6EEB`，其他页眉页脚文字固定为 `#3F4E63`，页脚分隔线固定为 `#D9E2F0`。
- 页眉页脚必须在首页和后续页保持一致，页码从 1 连续递增。本报告不得覆盖文案、坐标或颜色，也不得增加报告专属页眉、页脚、页码、Logo 或其他页面装饰。

## 中文字体契约

- Regular、Bold 必须来自两个不同的真实 PostScript 字形；不得把同一 Regular 同时注册为 normal/bold，不得依赖阅读器合成粗体、描边、重复绘制或字号模拟。
- macOS 首选 `/System/Library/Fonts/Supplemental/Songti.ttc`：Regular 为 `STSongti-SC-Regular`（子字体 6），Bold 为 `STSongti-SC-Bold`（子字体 1）；标题可用 `/System/Library/Fonts/STHeiti Medium.ttc` 的 `STHeitiSC-Medium`（子字体 1）。
- 其他系统必须显式提供覆盖报告全部字符的 Regular 与 Bold 字体文件；使用字体 cmap 预检字符覆盖，使用字体元数据核验 PostScript 名不同。
- ReportLab 必须分别注册 Regular/Bold 并建立字体族映射。动态文本先做 XML 转义；Markdown `**` 只有语义作用，必须转换为真实 Bold 字形。
- 正式排版前生成只含“正文测试”和“粗体测试”的临时 PDF，核验两种字体资源并目视确认差异。失败即停止 PDF 流程。

## 表格与紧凑字段

- 本报告必须在列定义层提供绝对 mm 列宽，所有列之和必须等于 165.9 mm（允许计算误差 0.05 mm）；不得按内容自动估算、缩放或重分配。
- ReportLab 使用通用工具提供的标准 `Table`、`colWidths` 与 `repeatRows=1`；不得改用启用长表优化的 `LongTable`，避免分片高度计算越过页边距并覆盖页眉。HTML 使用固定布局与绝对 mm `colgroup`；DOCX 同时固定 `tblGrid/gridCol` 与各列 `tcW`。
- 表头跨页重复时必须保持相同浅蓝实色底纹、黑色 Bold 字体和列宽。
- 允许表格自然跨页，允许过长单元格跨页拆分；不得为保住整行、整表或章节起始而压缩字号、行距或列宽。
- 编号、日期、短比例、短证据标签等紧凑字段不得逐字竖排。优先给足列宽；确需换行时只在 `｜`、`/`、空格等语义分隔处换行，不得对长叙述列全局禁用换行。

## 自然分页

- 不使用 `KeepTogether`、`keepWithNext`、整块容器或手动分页符保护章节、段落、说明或表格起始部分。
- 不因避免断句、孤立标题或来源文字而移动整块内容；跨页表格只重复表头，不重复小节标题。
- 不得通过缩小字号、字符缩放、压缩行距或压缩段间距减少页数。
- 不得出现裁切、重叠、乱码、黑块或强制分页造成的异常空白。除最后一页外，后续仍有连续内容而当前页空白超过可用正文区约三分之一时，判定失败。

## 验收与交付

1. 对照本报告语义验收清单确认 PDF 与最终 Markdown 内容源、章节、字段、表头、原值和免责声明一致；PDF 阶段不得改写业务内容。
2. 检查每页 MediaBox 为 Letter；检查文本可提取，必备标题/表头存在，且不存在未替换占位符。逐页检查统一页眉、页脚均可提取，页码与实际页序一致且连续。
3. 检查 PDF 字体资源包含不同的中文 Regular 与 Bold PostScript 字形；抽查标题、表头、正文和数据的实际字重。
4. 检查表头非描边填充色为 `#CED4EE`，跨页重复表头颜色一致；普通数据行无该填色。
5. 把全部页面渲染为 PNG，逐页检查字体、字号、行高、边距、页眉页脚、页码、颜色、列宽、换行、分页、裁切、重叠、乱码、黑块、异常空白和免责声明。
6. 任一步失败时删除不完整 PDF、字体测试 PDF、预览图片和其他本次临时文件，输出同一内容源的完整 Markdown。验收成功后只保留最终文件和明确要求的测试资产。

## 本地工具

- 使用 `../scripts/report_pdf_toolkit.py` 的 `discover_font_spec()`、`register_chinese_fonts()` 完成字体预检与注册。
- 使用 `make_styles()`、`make_table()` 和 `draw_page_chrome()` 生成固定样式、重复表头与统一页眉页脚。
- 使用 `verify_pdf()`、`render_pdf()` 完成结构、字体、文本和逐页图片验收；失败时调用 `guarded_validate_or_fallback()` 清理并回退完整 Markdown。
- 不得从 `../scripts/` 之外加载 PDF 工具；本 Skill 必须可以独立复制、运行和验证。

## 内容与层级

- PDF 与最终 Markdown 必须复用同一统一证据模型、`D` 派生文案、`EVIDENCE` 映射和条件删除结果；排版阶段不得重新归纳、删节、补造或改写。
- 主标题为“对公客户访前一页纸”；客户名称作为加粗居中副标题。报告编号、生成时间和密级作为居中元信息。
- 七章骨架、标题顺序、条件段落、数据来源汇总表和“报告使用说明”逐字遵循 `report-template.md`。不得增加封面或报告专属页面装饰；只启用本文件定义的统一页眉页脚与连续页码。
- 七章依次为核心观点、执行摘要、客户全景画像、产业画像与行业洞察、定制化营销方案、风险预警与合规提示、拜访建议与话题清单；不得将任一名称迁入通用层。
- 数据来源只显示骨架定义的一张两列表，并位于“报告使用说明”之前。

## 客户访前专属段落

- 核心观点固定保留 `1. 企业简介`、`2. 重点发展方向`、`3. 风险提示`、`4. 拜访目标` 四个粗体标签；五个动态正文均使用 Regular 并首行缩进两个正文汉字。企业简介定位段与可选沿革段分别缩进；沿革为 `null` 时删除条件行并合并空白。
- Markdown 中的两个全角空格只表达语义缩进；PDF 排版前移除，再由段落样式实现，禁止叠加缩进。
- 执行摘要“主要机会”和“主要风险”分别从 `1、` 开始连续编号，不用表格、圆点、短横线或自动列表。按本节最大序号计算统一悬挂缩进，续行正文与首行正文对齐；每条只保留一句精简正文及句末 OPP/RISK 编号。
- 信息解读和贷款视角解读正文首行缩进两个正文汉字；执行摘要其他项目不缩进。
- “报告使用说明”固定四条，逐条使用实心圆点 `•`、灰色 Regular 正文和约 5.5 pt 悬挂缩进，零项目间距；“报告目的：”“信息真实性：”“数据时效性：”“保密义务：”不得单独加粗。

## 专属粗体白名单

- 加粗：主标题、客户名称、章节标题、表头、核心观点四个编号标签、FACT/OPP/RISK 编号、骨架定义的各分节标签和各表字段名。
- Regular：五个核心观点动态字段、执行摘要正文、信息解读标签后的正文、表格数据、股东名、人员名、代表记录、风险说明、来源正文和报告使用说明。
- 禁止把带 `**` 的 Markdown 原样交给渲染器；静态标签与编号映射为真实 Bold，动态内容先转义再使用 Regular。

## 表格列定义

以下宽度是客户访前报告的业务配置，单位均为 mm，必须原值传给本 Skill 的本地表格构造器：

- 企业基本信息：35 / 130.9；关键决策人信息：27 / 38 / 100.9。
- 股东三列表：50 / 40 / 75.9；股东两列表：75 / 90.9。
- 有形资产：30 / 80 / 55.9；无形资产：34 / 28 / 103.9；核心经营数据：36 / 74 / 55.9。
- 行业周期与资金占用：27 / 35 / 29 / 38 / 36.9；行业对标与经营参照：26 / 30 / 34 / 30 / 45.9。
- 政策与融资环境：36 / 35 / 44 / 50.9；行业风险与贷款启示：25 / 32 / 36 / 34 / 38.9。
- 机会台账深化：18 / 28 / 45 / 44.9 / 30；贷款产品候选：35 / 36 / 30 / 30 / 34.9。
- 推进路径：24 / 30 / 32 / 45 / 34.9；风险预警：18 / 27 / 40 / 20 / 35.9 / 25。
- 推荐话题：30 / 50 / 60.9 / 25；关键问题：44 / 23 / 38 / 33.9 / 27。
- 建议取得资料：40 / 60 / 30 / 35.9；数据来源：30 / 135.9。

所有客户访前表格可见列的业务硬下限为 18 mm。新增骨架外表格原则上不得出现；确因用户要求新增时，先保证每列达到该下限，无法容纳时减少列数或拆表，不得等比压缩。

`OPP-01`、`RISK-01`、日期/“近期”、短证据标签、机会类型和风险类型属于紧凑字段，不得逐字竖排；只允许在 `｜`、`/` 或空格等语义边界主动换行。长叙述列保持自然换行。

## 专属语义验收

1. OPP 与 RISK 各自连续编号并在主台账、业务模块和拜访问题中闭环；机会类型、风险类型、证据状态、边界和核验方向满足本 Skill 的证据模型。
2. 核心观点四个标签各出现一次；企业简介候选、沿革条件、合作相关性、来源隐去和动态字段禁用话术继续执行 `derivation-and-generation-rules.md`，不得在 PDF 阶段修写。
3. 七章名称、编号和全部实际显示的表头与 `report-template.md` 一致；不存在空表、未替换占位符、内部字段路径或工具名。
4. 执行摘要两组编号列表均从 `1、` 开始，续行对齐；每条句末只出现一次真实 Bold 的 OPP/RISK 编号。
5. 逐表核对本文件列宽、紧凑字段、业务标签粗体和长单元格换行；跨页时只重复本文件定义的表头。
6. “报告使用说明”恰有四个圆点，四条正文均为 Regular；不得出现额外项目间距或标签加粗。

## 文件与回退

- 验收成功输出 `output/pdf/{company_name}-客户访前一页纸.pdf`。
- 用户指定 Markdown 或本 Skill 的 PDF 流程任一步失败时，删除本次不完整产物，并在聊天中交付 `report-template.md` 形成的完整 Markdown；不得只给摘要、路径或局部章节。
